//use uwuifythis::uwuifythis;

// TODO 1: implement this derive macro
/*#[uwuifythis]
fn foo() {
    let msg = "Hi, my name is Jakub. And what is your name?!";
    println!("{}", msg);
}

fn main() {
    foo();
}*/

// TODO 2: write a command-line application that
// uses clap to parse command-line arguments.
//
// It should read a directory passed as an argument and output basic information about it
// (files, directories, their names and sizes) into an output JSON file using serde[_json].

#[derive()]
struct CliArgs {}
