--- gthr_platform_linux.c
+++ gthr_platform_linux.c
@@ -74,6 +74,7 @@
   req.tv_sec = sec;
   req.tv_nsec = nanosec;
   while (nanosleep(&req, &req) != 0) {
+    // gt_platform_print("rem: %ld\n", req.tv_nsec);
     if (errno != EINTR)
       break;
     gt_yield();
